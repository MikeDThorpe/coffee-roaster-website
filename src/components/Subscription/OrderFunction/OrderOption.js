let OrderOption = ({title, description}) => {
    return (
        <section className="order-option">
            <h4>{title}</h4>
            <p>{description}</p>
        </section>
    )
}
export default OrderOption